application:
  env: development
  name: '@pom.name@'
  description: '@pom.description@'
  version: '@pom.version@'
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/albumstore?useSSL=false
    username: albumstore
    password: albumstore
  jpa:
    hibernate.ddl-auto: none
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
  flyway:
    check-location: true
    enabled: true
    url: jdbc:mysql://127.0.0.1:3306/albumstore?useSSL=false
    user: albumstore
    password: !@12QWqw
  jackson:
    default-property-inclusion: non_null
    serialization:
      fail-on-empty-beans: false